; This file is combined with dev.ini to enable hot module replacement mode.
;
; In "make devserver" + "make webpack" most files are served by the
; webpack-dev-server proxy. We set "hot" so that the templates don't link to CSS,
; as that is inlined into the JavaScript (required for hot module replacement).
[yarrharr]
hot = yes
