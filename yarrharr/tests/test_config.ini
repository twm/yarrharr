[yarrharr]
debug = yes
server_endpoint = tcp:8888:interface=127.0.0.1
external_url = http://127.0.0.1:8888
; Doesn't really matter for testing; files are served from app directories by
; Django's development server.
static_root = static/
static_url = /static/

[db]
name = testdb.sqlite

[secrets]
secret_key = supersekrit
